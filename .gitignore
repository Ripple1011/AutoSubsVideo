# --- Secrets (NEVER commit) -------------------------------------------------
.env
*.env
.env.*
*.env.*
backend/.env
backend/.env.local
backend/.env.txt
.env.local
.env.*.local

# Frontend build-time env. ONLY public, write-only keys belong here (PostHog
# project key, etc.). Vite inlines these into the client bundle at build,
# so anything sensitive (Razorpay secret, etc.) MUST stay in backend/.env.
!frontend/.env.production
!frontend/.env.example

# --- Python -----------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- Node / Vite ------------------------------------------------------------
node_modules/
frontend/dist/
frontend/.vite/
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# --- Runtime data (user uploads, job state, large media) --------------------
backend/data/

# --- OS junk ----------------------------------------------------------------
.DS_Store
Thumbs.db
desktop.ini

# --- IDE --------------------------------------------------------------------
.vscode/
.idea/
*.swp
*.swo

# --- Logs / temp ------------------------------------------------------------
*.log
*.tmp
*.bak

# --- Local-only working files (not part of the project) ---------------------
.claude/
Prompts/
